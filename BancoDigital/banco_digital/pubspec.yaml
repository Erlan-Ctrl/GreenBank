name: banco_digital
description: "A new Flutter project."
publish_to: 'none' # Remove esta linha se quiser publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Ícones no estilo iOS
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Lint recomendada pelo Flutter
  flutter_lints: ^5.0.0

flutter:
  # Habilita ícones do Material Design
  uses-material-design: true

  # Imagens e recursos usados no app
  assets:
    - assets/profile.jpg
    - assets/banner_forest.png
    - assets/work_in_progress.png

dependencies:
  http: ^0.13.5
  qr_code_scanner: ^1.0.1